apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: soccerdata-pvc
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - {{ .Values.services.dataIngestion.storage.accessMode }}
  resources:
    requests:
      storage: {{ .Values.services.dataIngestion.storage.size }}
